<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--对话的基本样式-->
    <style name="BaseDialogTheme" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
    <!--加了上下弹出动画的对话样式-->
    <style name="UdDialogTheme" parent="BaseDialogTheme">
        <item name="android:windowAnimationStyle">@style/UpDownAnimation</item>
    </style>
    <!--从底部弹出，收回的动画-->
    <style name="UpDownAnimation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/anim_popup_up</item>
        <item name="android:windowExitAnimation">@anim/anim_popup_down</item>
    </style>

</resources>


